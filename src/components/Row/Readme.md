Row start prop
```
const styles = {
  backgroundColor: '#efeeed',
  textAlign: 'center',
  padding: '1rem',
  height: '3rem'
};
<Grid fluid>
  <Row start='xs'>
    <div style={styles}>
      start
    </div>
  </Row>
</Grid>
```
Row center prop
```
const styles = {
  backgroundColor: '#efeeed',
  textAlign: 'center',
  padding: '1rem',
  height: '3rem'
};
<Grid fluid>
  <Row center='xs'>
    <div style={styles}>
      center
    </div>
  </Row>
</Grid>
```
Row end prop
```
const styles = {
  backgroundColor: '#efeeed',
  textAlign: 'center',
  padding: '1rem',
  height: '3rem'
};
<Grid fluid>
  <Row end='xs'>
    <div style={styles}>
      end
    </div>
  </Row>
</Grid>
```
